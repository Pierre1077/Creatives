const h1 = document.querySelector('h1');
var texte = h1.textContent;

console.log(texte);

/*const intro = 'Voici un exemple pour une énigme, en gros le gros texte c est l intro qui introduit le contexte de l enigme. Après y a des dialogues pour un peu d intéraction avec le joueur';
const text = 'Salut moi c kilyan le mec qui a encore rater un peanlty hier';
const text2 = 'Je tient à dire que thibaut est très beauuu !';

//p1.innerHTML = text;
//p2.innerHTML = text2;

let i = 0;
let j = 0;
const interval = setInterval(() => {
    h1.innerHTML += intro[i];
    i++;
    if (i >= intro.length){
        clearInterval(interval);
    }
}, 50)*/
